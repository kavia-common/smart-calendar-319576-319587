# Optional: override API base URL (default is same-origin)
# Example: https://vscode-internal-20465-beta.beta01.cloud.kavia.ai:3001
REACT_APP_API_BASE_URL=

# Optional: override events base path (default /api/events, fallback tries /events)
REACT_APP_API_EVENTS_BASE=/api/events
